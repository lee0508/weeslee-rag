# 2026-05-21 서버 시스템 분석 보고서

## 1. 점검 개요

- 점검 대상 서버: `192.168.0.207`
- 접속 계정: `weeslee`
- 프로젝트 경로: `/data/weeslee/weeslee-rag`
- 점검 시각: `2026-05-21 07:29 KST` 기준
- 점검 방식: SSH 원격 조회 명령 실행

## 2. 시스템 상태

| 항목 | 결과 |
| --- | --- |
| Hostname | `weeslee` |
| OS | Ubuntu 24.04.4 LTS |
| Kernel | `6.8.0-106-generic` |
| Architecture | `x86_64` |
| Uptime | `50 days, 17:46` |
| 접속 사용자 | `6 users` |
| Load Average | `0.11, 0.46, 0.61` |

판단.

- 장기 가동 중이며 현재 시스템 부하는 낮다.
- 재부팅 필요 플래그가 존재하므로 커널 업데이트 적용을 위해 운영 시간 조율 후 재부팅이 필요하다.

## 3. 프로세서와 자원 상태

| 항목 | 결과 |
| --- | --- |
| CPU | AMD Ryzen 9 7900 12-Core Processor |
| 코어와 스레드 | 12코어, 24스레드 |
| 온라인 CPU | `0-23` |
| 메모리 | 총 `61Gi`, 사용 `5.5Gi`, 가용 `56Gi` |
| Swap | 총 `8.0Gi`, 사용 `92Mi`, 여유 `7.9Gi` |

`vmstat 1 3` 기준 CPU idle은 98%에서 100% 범위였다.

판단.

- CPU와 메모리는 현재 여유가 충분하다.
- Swap 사용량은 낮아 메모리 압박 상태로 보이지 않는다.
- 현재 RAG, OCR, 웹 서비스가 동시에 떠 있어도 자원 병목은 디스크 쪽에 더 가깝다.

## 4. 디스크 용량

| 파일 시스템 | 마운트 | 크기 | 사용 | 여유 | 사용률 |
| --- | --- | ---: | ---: | ---: | ---: |
| `/dev/mapper/ubuntu--vg-ubuntu--lv` | `/` | `98G` | `86G` | `7.1G` | `93%` |
| `/dev/mapper/ubuntu--vg-data` | `/data` | `814G` | `292G` | `482G` | `38%` |
| `//192.168.0.56/W2_프로젝트폴더` | `/mnt/w2_project` | `3.6T` | `2.1T` | `1.5T` | `59%` |
| `/dev/nvme0n1p2` | `/boot` | `2.0G` | `297M` | `1.5G` | `17%` |
| `/dev/nvme0n1p1` | `/boot/efi` | `1.1G` | `6.2M` | `1.1G` | `1%` |

루트 파티션 주요 사용량.

| 경로 | 사용량 |
| --- | ---: |
| `/home` | `48G` |
| `/usr` | `20G` |
| `/var` | `7.3G` |
| `/opt` | `397M` |

`/home/weeslee` 주요 사용량.

| 경로 | 사용량 |
| --- | ---: |
| `/home/weeslee/.cache` | `44G` |
| `/home/weeslee/miniconda3` | `1.5G` |
| `/home/weeslee/.npm` | `954M` |
| `/home/weeslee/.hermes` | `842M` |

`/home/weeslee/.cache` 주요 사용량.

| 경로 | 사용량 |
| --- | ---: |
| `/home/weeslee/.cache/huggingface` | `26G` |
| `/home/weeslee/.cache/pip` | `16G` |
| `/home/weeslee/.cache/uv` | `780M` |
| `/home/weeslee/.cache/ms-playwright` | `631M` |

`/var/log` 주요 사용량.

| 경로 | 사용량 |
| --- | ---: |
| `/var/log` | `4.2G` |
| `/var/log/journal` | `3.0G` |
| systemd journal 전체 | `2.9G` |

판단.

- 가장 큰 위험은 루트 파티션 `/`의 93% 사용률이다.
- 즉시 장애 상태는 아니지만 패키지 업데이트, 로그 증가, 모델 캐시 증가가 겹치면 루트 파티션이 빠르게 포화될 수 있다.
- 프로젝트 데이터 파티션 `/data`는 여유가 충분하다. 대용량 산출물, 모델, 인덱스, 임시 처리 데이터는 `/data` 하위에 두는 운영 원칙을 유지해야 한다.

## 5. 주요 프로세스

CPU 사용률 상위 프로세스 기준 주요 항목.

| 프로세스 | 상태 |
| --- | --- |
| `uvicorn app.main:app --port 8080` | 실행 중, RAG FastAPI |
| `mysqld` | 실행 중 |
| `mongod --bind_ip_all` | 실행 중 |
| `ollama serve` | 실행 중 |
| `PM2 v6.0.14` | 실행 중 |
| `next-server` | 실행 중 |
| `advanced_ocr_server.py` | 실행 중 |

판단.

- 현재 CPU를 지속적으로 점유하는 프로세스는 없다.
- `weeslee-rag` 관련 FastAPI는 8080과 8002 포트에서 각각 프로세스가 확인된다. 운영 라우팅 기준 포트가 무엇인지 문서화해 중복 기동을 관리하는 것이 좋다.

## 6. 웹서버와 서비스 상태

systemd 기준 running 서비스.

| 서비스 | 상태 |
| --- | --- |
| `nginx.service` | active running |
| `docker.service` | active running |
| `mysql.service` | active running |
| `ollama.service` | active running |
| `ssh.service` | active running |

추가 확인 결과.

| 항목 | 결과 |
| --- | --- |
| HTTP 80 | `302` |
| HTTPS 443 | `302` |
| FastAPI 8080 | `/api/health` 기준 `HTTP 200`, `{"status":"healthy"}` |
| Nginx 메모리 | 약 `38.5M` |
| Nginx uptime | 5일 이상 |

PM2 상태.

| 이름 | 상태 | 메모리 |
| --- | --- | ---: |
| `drug-system` | online | `101.7mb` |
| `jupiter2` | online | `922.2mb` |
| `kc-monitor` | online | `54.8mb` |
| `tajik-task-frontend2-api` | online | `72.2mb` |
| `weeslee-rag` | online | `150.0mb` |

Docker 컨테이너.

| 이름 | 상태 | 포트 |
| --- | --- | --- |
| `drug_postgres` | Up 7 weeks | `5432` |
| `mongodb` | Up 7 weeks | `27017` |

실패 서비스.

| 서비스 | 상태 |
| --- | --- |
| `jupyter.service` | failed |
| `systemd-networkd-wait-online.service` | failed |

판단.

- 웹서버와 핵심 서비스는 정상 running 상태다.
- `jupyter.service`가 실제 운영에 필요하면 복구가 필요하고, 필요하지 않으면 비활성화해 실패 알림 노이즈를 줄이는 것이 좋다.
- `systemd-networkd-wait-online.service` 실패는 부팅 시 네트워크 대기 관련 실패로 보이며, 실제 네트워크와 서비스는 현재 동작 중이다.

## 7. 리스닝 포트 요약

주요 포트.

| 포트 | 용도 추정 |
| ---: | --- |
| `21` | FTP |
| `22` | SSH |
| `80` | HTTP, Nginx |
| `443` | HTTPS, Nginx |
| `3306` | MySQL localhost |
| `5432` | PostgreSQL Docker |
| `27017` | MongoDB Docker |
| `8080` | `weeslee-rag` FastAPI |
| `8002` | `weeslee-rag` PM2 FastAPI |
| `8098` | `kc-monitor` |
| `11434` | Ollama |

판단.

- 외부 공개 포트가 많다. 방화벽 정책과 Nginx 라우팅 기준으로 실제 외부 노출 필요성을 정기 점검하는 것이 좋다.
- MongoDB와 PostgreSQL이 `0.0.0.0`에 바인딩되어 있으므로, 내부망 전용이라도 방화벽 제한 여부를 별도로 확인해야 한다.

## 8. 시스템 업데이트 상태

| 항목 | 결과 |
| --- | --- |
| apt 목록 갱신 시각 | `2026-05-21 02:10:03 KST` |
| 업데이트 대기 패키지 | `82개` |
| 현재 커널 | `6.8.0-106-generic` |
| 재부팅 필요 여부 | 필요 |

업데이트 대기 주요 패키지군.

- Docker 계열: `docker-ce`, `docker-ce-cli`, `docker-compose-plugin`, `containerd.io`
- 보안과 시스템 계열: `apparmor`, `coreutils`, `iproute2`
- 개발 도구 계열: `binutils`, `git-lfs`
- 데스크톱과 드라이버 계열: `fwupd`, `google-chrome-stable`, `libgtk-4-*`
- 커널 관련 재부팅 필요 패키지: `linux-image-6.8.0-107-generic`, `linux-image-6.8.0-110-generic`, `linux-image-6.8.0-111-generic`, `linux-image-6.8.0-117-generic`

판단.

- 패키지 업데이트가 상당히 밀려 있다.
- Docker와 커널 업데이트가 포함되어 있으므로 운영 중 즉시 업데이트보다는 유지보수 시간을 잡아 진행해야 한다.
- 업데이트 후 재부팅과 서비스 자동 복구 확인이 필요하다.

## 9. 위험도와 권장 조치

| 우선순위 | 항목 | 권장 조치 |
| ---: | --- | --- |
| 높음 | 루트 파티션 93% 사용 | `/home/weeslee/.cache/huggingface`, `/home/weeslee/.cache/pip` 정리 여부 확인 후 캐시 정리 |
| 높음 | 재부팅 필요 | 운영 영향 시간대 확정 후 업데이트와 재부팅 수행 |
| 중간 | systemd journal 2.9G | journal 보관 정책 설정 또는 vacuum 정리 |
| 중간 | 업데이트 82개 대기 | Docker, 커널 포함 여부를 고려해 점검 창구에서 업데이트 |
| 중간 | 실패 서비스 2개 | `jupyter.service` 필요 여부 확인 후 복구 또는 비활성화 |
| 중간 | 외부 리스닝 포트 다수 | UFW, Nginx, Docker 포트 노출 정책 재점검 |
| 낮음 | `/data` 여유 충분 | 대용량 데이터와 모델 캐시는 `/data`로 이전하는 정책 유지 |

## 10. 결론

서버의 CPU, 메모리, 주요 웹서비스 상태는 안정적이다. 현재 즉시 대응이 필요한 핵심 리스크는 루트 파티션 용량이다. `/home/weeslee/.cache`에 모델과 패키지 캐시가 크게 쌓여 있으므로 정리 대상 확인이 필요하다.

운영 관점에서는 패키지 업데이트 82개와 재부팅 필요 상태가 함께 확인되었다. 업데이트에는 Docker와 커널 계열이 포함되어 있으므로 서비스 중단 가능성을 고려해 유지보수 시간에 처리하는 것이 적절하다.

프로젝트 데이터 파티션 `/data`는 여유가 충분하다. 앞으로도 RAG 원본, 인덱스, 모델, 임시 산출물은 루트가 아니라 `/data` 하위에 두는 운영 원칙을 유지해야 한다.
