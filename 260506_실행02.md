오늘 작업 결과를 종합하면 현재 프로젝트는:

"검색 가능한 문서 저장소"
단계를 넘어서

"운영 가능한 컨설팅 RAG"
단계로 진입

했다고 판단됩니다.

특히 중요한 점은:

batch-003 추가 후에도
검색 품질 회귀(regression)가 없었다

는 점입니다.

이건 매우 중요합니다.

왜냐하면 대부분의 RAG 시스템은:

문서 수 증가
→ 노이즈 증가
→ 검색 품질 하락

이 발생하는데,

현재 시스템은:

27 docs → 61 docs
2,054 vectors → 6,760 vectors

확장 후에도:

평균 키워드 적중률 95%
유지

되었기 때문입니다.

현재 상태 분석

현재 완료 상태:

✓ batch-002 완료
✓ batch-003 완료
✓ combined index 구축
✓ project_name rerank
✓ category pre-filter
✓ max_chunks_per_doc
✓ nomic embedding
✓ 검색 품질 검증

즉 지금부터는:

"검색 엔진 개발"

보다:

"운영 안정화 + 지식 계층 추가"

가 중심입니다.

가장 먼저 작업해야 할 것
1️⃣ OCR 파이프라인 분리 구축

이게 가장 중요합니다.

문서 분석 결과 현재 실패 4건은 모두:

스캔 PDF

입니다.

즉 현재 시스템의 가장 큰 약점은:

텍스트 PDF는 강함
스캔 PDF는 아직 약함

입니다.

왜 OCR이 중요한가?

현재 위즐리 실제 문서 환경은:

발표자료
착수보고
중간보고
최종보고

중 상당수가:

스캔 PDF

입니다.

즉 OCR이 안정화되지 않으면:

실제 운영 시 coverage 부족

이 발생합니다.

추천 구조

현재 extract pipeline:

PDF
→ pdfplumber
→ text extract

추천 변경:

PDF
→ text layer 존재?
    YES → pdfplumber
    NO  → OCR pipeline
추천 작업 순서
오늘 밤 / 내일 오전
tesseract --version

확인.

그 다음:

sudo apt install tesseract-ocr
pip install pytesseract pdf2image

(회사 서버 권한 여부 확인 필요)

두 번째 중요 작업
2️⃣ category sub-index 구축

이건 반드시 해야 합니다.

현재는:

single FAISS

입니다.

하지만 이미 문서에서:

rfp/proposal/kickoff/final_report/presentation

서브 인덱스를 계획했습니다.

왜 중요한가?

현재 문제:

RFP 질의
→ proposal 결과

가 일부 존재합니다.

즉 현재는 semantic similarity가 강하지만:

intent awareness

가 부족합니다.

추천 구조
indexes/
├── combined/
├── rfp/
├── proposal/
├── kickoff/
├── final_report/
└── presentation/
효과

질문:

"제안요청서 요구사항"

이면:

rfp index 우선 검색

이 가능해집니다.

이 순간부터:

semantic retrieval

이 아니라:

intent-aware retrieval

이 됩니다.

세 번째 중요 작업
3️⃣ Project Wiki 자동 생성

이제 시작해도 됩니다.

왜냐하면 현재:

retrieval quality

가 어느 정도 안정화되었습니다.

특히:

project_name rerank

가 이미 구현되었습니다.

이건 매우 중요합니다.

추천 방식

절대:

chunk → wiki

로 하지 마세요.

반드시:

project → wiki

로 가야 합니다.

추천 생성 구조
project
→ top chunks
→ summarize
→ structured markdown
→ wiki/projects/
추천 생성 순서

우선 생성 추천:

1. 한국수자원공사
2. AX 기반 차세대 ISMP
3. 법무부 성범죄백서 이행
네 번째 중요 작업
4️⃣ metadata 품질 점수 시스템

이건 꼭 추가하세요.

현재 metadata 정규화는 시작되었지만:

신뢰도(score)

가 없습니다.

추천 구조
{
  "organization": "한국수자원공사",
  "organization_confidence": 0.95,

  "project_name": "K-water ISP",
  "project_confidence": 0.88
}
왜 중요한가?

나중에 graph/wikification 단계에서:

잘못된 entity 연결

을 막기 위해 매우 중요합니다.

다섯 번째 중요 작업
5️⃣ retrieval evaluation 자동화

현재는 사람이 평가합니다.

하지만 지금부터는:

자동 benchmark

가 필요합니다.

추천 구조
tests/
├── rfp_queries.json
├── proposal_queries.json
├── kickoff_queries.json
└── evaluation.py
자동 평가 예시
{
  "query": "ISP 사업 제안요청서 요구사항",
  "expected_keywords": [
    "요구사항",
    "과업범위",
    "추진전략"
  ],
  "expected_category": "rfp"
}
여섯 번째 중요 작업
6️⃣ incremental indexing

지금은:

전체 rebuild

입니다.

하지만 실제 운영에서는:

새 문서만 추가

가 필요합니다.

추천 구조
manifest
→ changed docs only
→ embed
→ append vectors
일곱 번째 중요 작업
7️⃣ provenance 계층 구현

현재 문서에서 아직 미구현입니다.

하지만 이건 LLM-Wiki의 핵심입니다.

추천 구조
{
  "value": "ISP",
  "source": "DOC-20260506-000022",
  "provenance": "EXTRACTED"
}
현재 프로젝트의 가장 중요한 전환점

현재 프로젝트는:

검색 시스템

에서:

Knowledge Operating System

으로 넘어가는 중입니다.

현재 우선순위 최종 추천
이번 주
반드시
1. OCR pipeline
2. category sub-index
3. benchmark automation
4. metadata confidence
다음 주
5. Project Wiki
6. Organization Wiki
7. incremental indexing
그 이후
8. graph linkage
9. explainable recommendation
10. contradiction tracking
가장 중요한 최종 결론

현재 문서들을 종합하면:

검색 품질은 이미 상당 수준 확보

되었습니다.

이제 진짜 중요한 것은:

"지속적으로 축적 가능한
컨설팅 지식 시스템"

으로 발전시키는 것입니다.

그리고 그 첫 번째 핵심 작업은:

OCR 안정화
+
Project Wiki 계층 추가

입니다.