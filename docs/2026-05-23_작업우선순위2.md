4. 사용자 UX와 관리자 UX의 연결 관계

사용자 화면에서 보이는 데이터는 관리자 화면에서 생성되어야 한다.

Phase 5 데이터 매핑표는 사용자 화면 데이터와 관리자 생성 데이터의 연결 관계를 정의하며, 예를 들어 질문 분석은 Prompt Analyzer에서 실시간 생성되고, RAG 결과는 FAISS와 chunks.jsonl에서, Graph RAG 결과는 nodes.jsonl/edges.jsonl에서, LLM Wiki 결과는 data/wiki/*.md에서 제공된다고 정리합니다.

관리자 화면에서 생성
    ↓
documents.jsonl
chunks.jsonl
FAISS Index
nodes.jsonl
edges.jsonl
wiki/*.md
summaries/*.md
    ↓
사용자 화면에서 검색/표시
4.1 데이터 생성 → 사용자 화면 매핑
사용자 화면	필요한 데이터	관리자 생성 단계
검색 결과	chunk_text, score, metadata	Chunking, Embedding, FAISS Build
Graph 결과	graph_path, connected_entities	Graph RAG Build
Wiki 결과	title, summary, related_documents	LLM Wiki Build
문서 상세	html, markdown, summary, metadata	Text Extraction, Summary
Grounded Answer	selected_docs, citations	LLM Service, chunks.jsonl
5. 개발 속도 개선을 위한 UX 반영 사항
5.1 전체 문서 RAG보다 Sample RAG 우선

현재 개발 속도 지연의 핵심은 전체 문서를 한 번에 RAG화하려는 것이다.

따라서 관리자 UX에 반드시 다음 기능을 넣어야 한다.

Sample RAG Builder
- 폴더별 샘플 30개 추출
- 우선 문서 유형 선택
- 제외 폴더 설정
- 샘플 Collection 생성
- 검색 품질 테스트
5.2 사용자 화면은 Sample Collection부터 연결

사용자 화면에서는 우선 다음 Collection을 기본값으로 사용한다.

weeslee_sample_rag_v1

이후 검색 품질이 검증되면 전체 Collection으로 확장한다.

1단계: weeslee_sample_rag_v1
2단계: weeslee_department_rag_v1
3단계: weeslee_full_rag_v1