# weeslee-rag의 UX는 다음 원칙으로 정리해야 한다

# 사용자 UX 원칙

쉽게 질문한다.
검색 결과를 먼저 본다.
문서를 직접 선택한다.
선택한 문서만 기반으로 답변을 생성한다.
답변에는 반드시 출처를 표시한다.

# 관리자 UX 원칙

문서를 스캔한다.
메타데이터를 만든다.
텍스트를 추출한다.
Chunk를 만든다.
Embedding한다.
FAISS를 만든다.
Graph와 Wiki를 만든다.
Job과 오류를 관리한다.
검색 품질을 검증한다.

[2026-05-23_작업우선순위.md]