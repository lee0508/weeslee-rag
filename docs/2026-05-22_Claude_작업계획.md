# 2026-05-22 Claude 작업 계획

## 1. 오늘 완료된 작업 분석

### 1.1 작업 로그 요약 (총 16 step 완료)

| Step | 작업 내용 | 파일 |
|------|----------|------|
| 01-04 | admin.html Docs 레이아웃 보강 + 원격 배포 | admin.html, admin-docs-layout.js/css |
| 05-08 | Source Documents 페이지 구현 | admin-docs-layout.js |
| 09 | FAISS Index 페이지 요약 연결 | admin-docs-layout.js |
| 10 | JSON Graph 페이지 요약 연결 | admin-docs-layout.js |
| 11-12 | Wizard Collection 메타 동기화 | admin-docs-layout.js |
| 13-14 | 폴더 기준 Collection 생성 로직 | rag_source_pipeline.py, rag_source_admin.py |
| 15 | 답변 근거 파일 노출 (evidence_documents) | rag.py, rag-assistant.html |
| 16 | 관리자 검색 테스트 경로 노출 | rag.py, admin.html |

### 1.2 변경된 파일 (총 9개, +789/-90 lines)

```
backend/app/api/rag.py                        |  71 +++--
backend/app/api/rag_source_admin.py           |  83 +++++
backend/app/services/rag_source_pipeline.py   |  14 +-
frontend/admin.html                           |  94 +++++
frontend/assets/css/admin-docs-layout.css     |  31 ++
frontend/assets/js/admin/admin-docs-layout.js | 392 ++++++
frontend/rag-assistant.html                   | 177 ++++++
SETUP_LOCAL.md                                |   2 +-
.claude/settings.json                         |  15 +-
```

### 1.3 핵심 기능 추가 내역

1. **답변 근거 파일 노출** (`rag.py`)
   - `/api/rag/query` 응답에 `evidence_documents`, `retrieval_summary` 추가
   - `/api/rag/answer` 응답에 `evidence_documents` 포함
   - 사용자가 "어떤 문서를 근거로 삼았는지" 즉시 확인 가능

2. **관리자 검색 테스트 경로 노출** (`rag.py`)
   - `/api/rag/search` 결과에 `project_name`, `file_name`, `source_path`, `original_source_path`, `relative_path`, `collection_key` 추가
   - 관리자 화면에서 검색 품질과 데이터 정합성 즉시 확인 가능

3. **폴더 기준 Collection 생성** (`rag_source_pipeline.py`)
   - `collection_key`를 파일 직접 상위 폴더명 기준으로 변경
   - `collections/bootstrap`가 `metadata.db` 문서 경로에서 폴더명별 동기화

4. **admin.html Docs 레이아웃** (`admin-docs-layout.js`)
   - RAG Build Wizard, Logs, Search Quality 페이지 추가
   - FAISS Index, JSON Graph, Source Documents 요약 카드 연결
   - 페이지별 API 요약 데이터 로딩

5. **rag-assistant.html 개선** (`rag-assistant.html`)
   - 답변 탭에서 근거 파일명, 경로, 열기/다운로드 액션 표시
   - 검색 전용 모드에서도 근거 파일 확인 가능

---

## 2. 원격 서버 상태

- **서버**: `weeslee@218.148.21.12:2222`
- **경로**: `/data/weeslee/weeslee-rag`
- **Git 상태**: `ba2bbc8` (main, origin/main과 동기화됨)
- **로컬 변경**:
  - `data/active_index.json` (수정됨)
  - `data/wiki/projects/k-water-isp.md` (수정됨)
  - `data/extracted_text/` (추적 안됨)
  - `data/rag_source_metadata.jsonl` (추적 안됨)
  - `platform_config/` (추적 안됨)
- **프로세스**: PID 3670675 (uvicorn, 8080 포트)
- **헬스체크**: `{"status":"healthy"}`

---

## 3. 로컬 상태

- **경로**: `C:\xampp\htdocs\weeslee-rag`
- **Git 상태**: `ba2bbc8` (main, 커밋 안된 변경 9개 파일)
- **상태**: 원격과 base commit 동일, 로컬에 오늘 작업 변경사항 미커밋

---

## 4. 남은 리스크 및 미완료 작업

### 4.1 식별된 리스크 (로그 기반)

| 리스크 | 상태 | 대응 방안 |
|--------|------|----------|
| `rag-assistant.html` 기존 인코딩/문자열 손상 | 미해결 | 손상 구간 정리 필요 |
| `admin.html` 전체 script 문법 검증 | 미수행 | 브라우저 테스트 필요 |
| 직접 상위 폴더명 ≠ 프로젝트 루트 폴더명 | 미해결 | Collection 단위 해석 재정의 |
| LLM 프롬프트에 "근거 파일명 명시" 규칙 미적용 | 미완료 | Phase 7 환각방지 정책 적용 필요 |

### 4.2 오늘 작업 대비 미완료 항목 (2일 개발 계획서 기준)

| 항목 | 상태 | 비고 |
|------|------|------|
| POST /api/rag-assistant/generate-grounded-answer | ❌ 미구현 | evidence_documents 추가됨, 별도 엔드포인트 필요 |
| POST /api/rag-assistant/analyze-prompt | ❌ 미구현 | 프롬프트 분석 API |
| rag-assistant.html 4탭 결과 UI | ⚠️ 부분 | 근거 파일 표시 추가됨, 탭 분리 미완료 |
| Grounded Answer + Citations | ⚠️ 부분 | evidence_documents 추가됨, Citation 형식 미적용 |
| admin.html RAG Build Wizard Stepper | ⚠️ 부분 | 페이지 추가됨, 9단계 Stepper 미완성 |

---

## 5. 오늘 작업 계획 (우선순위 순)

### P0 - 필수 (오전)

1. **로컬 변경사항 커밋 및 푸시**
   - 오늘 완료된 16 step 작업 커밋
   - 원격 서버 pull 및 서비스 재시작

2. **브라우저 E2E 테스트**
   - `admin.html` Docs 레이아웃 확인
   - `rag-assistant.html` 근거 파일 표시 확인
   - 관리자 검색 테스트 경로 표시 확인

3. **rag-assistant.html 인코딩 손상 수정**
   - 손상된 문자열 구간 식별 및 수정

### P1 - 높음 (오후)

4. **Grounded Answer API 구현**
   - POST /api/rag-assistant/generate-grounded-answer
   - Phase 7 환각방지 정책 시스템 프롬프트 적용
   - Citation 형식 ([1], [2]) 구현

5. **Prompt Analysis API 구현**
   - POST /api/rag-assistant/analyze-prompt
   - 규칙 기반 intent, keywords, filters 추출

### P2 - 중간 (시간 여유 시)

6. **admin.html RAG Build Wizard Stepper 완성**
   - 9단계 상태 관리
   - 단계별 UI 컴포넌트

7. **rag-assistant.html 4탭 결과 분리**
   - RAG / Agent / Graph / Wiki 탭 분리

---

## 6. 작업 지시 대기

위 계획을 기반으로 작업 지시를 대기합니다.

**권장 시작점**: P0-1 (커밋 및 푸시)로 오늘 완료된 작업을 확정하고, P0-2 (브라우저 테스트)로 동작 확인 후 P1 작업 진행.

---

작성일: 2026-05-22
작성자: Claude
상태: 작업 지시 대기
