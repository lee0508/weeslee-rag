# 2026-04-27 실제 RFP 기반 추천 품질 검증

## 1. 대상 RFP

- 문서: `AX 기반의 차세대 업무시스템 구축을 위한 ISMP 제안요청서`
- 원본 형식: `HWPX`
- 원본 위치:
  `data/raw/snapshot_2026-04-27/domestic_business/202603. AX기반의 차세대 업무 시스템 구축을 위한 ISMP/00. RFP/1. 제안요청서.hwpx`

## 2. 검증 방식

HWPX 내부 XML과 미리보기 텍스트를 확인한 뒤, 실제 요구사항을 반영한 질의로 RAG 조립 파이프라인을 실행했다.

사용한 핵심 질의어:

- `AX 기반의 차세대 업무시스템 구축`
- `제안요청서`
- `SW 산출물 활용촉진`
- `지적재산권 공동귀속`
- `원격지 개발 장소 보안요구사항`
- `프로젝트 관리`
- `일정관리`

## 3. 실행 결과

실행 결과 상위 문서는 다음과 같았다.

1. `DOC-20260427-000007` `proposal`
2. `DOC-20260427-000010` `presentation`
3. `DOC-20260427-000024` `final_report`
4. `DOC-20260427-000017` `proposal`
5. `DOC-20260427-000019` `final_report`

## 4. 판단

### 4.1 잘 맞는 점

1. 질의 의도와 일치하는 `proposal` 문서가 1순위로 올라왔다.
2. `프로젝트 관리`, `일정관리`, `보안요구사항`, `산출물 활용` 관련 근거가 함께 묶였다.
3. `gemma4` 초안 응답도 정상적으로 생성됐다.

### 4.2 개선할 점

1. 실제 RFP 문서가 `HWPX`라서, 추출/질의 정규화를 더 강하게 해야 한다.
2. 현재 `proposal` / `presentation` / `final_report` 문서가 많아, `RFP` 자체와의 직접 연결을 강화해야 한다.
3. `RFP -> proposal` 관계를 그래프 구조로 명시하면 추천 설명력이 좋아진다.

## 5. 결론

실제 RFP 기반 검증은 성공적으로 수행됐다.
현재 파이프라인은 RFP 질의의 핵심 요구사항을 반영한 추천과 초안 생성을 만들 수 있다.

다음 단계에서 필요한 작업은 아래와 같다.

1. `RFP` 직접 추출 강화
2. `proposal`과의 관계 매핑 강화
3. 문서 유형별 reranking 고도화
4. 그래프 기반 explainability 추가
